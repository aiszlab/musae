import { ComponentProps } from "../../types/element";

/**
 * @description
 * rate props
 */
export type RateProps = ComponentProps & {
  /**
   * @description
   * rate count
   * @default 5
   */
  count?: number;
};
