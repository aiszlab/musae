import { ComponentProps } from "../../types/element";

/**
 * @description
 * quote props
 */
export type QuoteProps = ComponentProps & {
  /**
   * @description
   * children
   */
  children?: string;
};
