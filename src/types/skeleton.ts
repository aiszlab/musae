import type { ComponentProps } from "musae/types/element";

/**
 * @description
 * skeleton props
 */
export type SkeletonProps = ComponentProps & {
  /**
   * @description
   * animation
   * @default true
   */
  animation?: boolean;
};
