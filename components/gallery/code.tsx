import React from "react";
import { useClassNames } from "../config";
import { ComponentToken } from "../../utils/class-name";

const GalleryCode = () => {
  const classNames = useClassNames(ComponentToken.Gallery);

  return <div></div>;
};

export default GalleryCode;
